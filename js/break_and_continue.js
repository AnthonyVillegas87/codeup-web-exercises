(function() {
    "use strict"

    let num = 0
        while(num < 50) {
            num = prompt("Pick an odd number between 1 & 50!");
                if(num % num === 0) break;

        }







})()
